{
    "configurations": [
    {
     "type": "lldb",
     "request": "launch",
     "name": "Launch current exec",
     "program": "${workspaceFolder}/a.out",
     "args": [],
     "cwd": "${workspaceFolder}",
     "stopOnEntry": false
    }
    ]
}